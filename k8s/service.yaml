apiVersion: v1
kind: Service
metadata:
  name: aws-devops-dashboard
  namespace: default
spec:
  selector:
    app: aws-devops-dashboard
  ports:
  - protocol: TCP
    port: 80
    targetPort: 3000
  type: ClusterIP
